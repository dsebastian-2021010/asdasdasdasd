kind = "api"
previewPath = "/api/accounts"
title = "Bank Service"
version = "1.0.0"

[[services]]
localPort = 3000
name = "Bank Service"
paths = ["/api/accounts", "/api/movements", "/api/checks", "/docs"]

[services.env]
PORT = "3000"
NODE_ENV = "development"

[services.development]
run = "pnpm --filter Bank-service run dev"
